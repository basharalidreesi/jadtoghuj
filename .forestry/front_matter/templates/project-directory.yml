---
label: Project Directory
hide_body: false
fields:
- name: projects
  type: field_group_list
  fields:
  - name: project_name
    type: text
    config:
      required: true
    label: Project name
    default: ''
  - name: project_description
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: markdown
    label: Project description
  - name: looks
    type: field_group
    config: {}
    fields:
    - name: look
      type: field_group_list
      fields:
      - name: look_name
        type: text
        config:
          required: false
        label: Look name
      - name: look_cover
        type: file
        config:
          maxSize: 64
        label: Look cover
      - name: look_images
        type: image_gallery
        config: {}
        label: Look images
      config:
        min: 
        max: 
        labelField: look_name
      label: Look
    label: Looks
  config:
    min: 
    max: 
    labelField: project_name
  label: Projects
