---
label: Homepage Showcase
hide_body: true
fields:
- name: entries
  type: field_group_list
  fields:
  - name: entry_image
    type: file
    config:
      maxSize: 2.5
    label: Entry image
  - name: entry_link
    type: text
    config:
      required: false
    label: Entry link
  config:
    min: 
    max: 
    labelField: 
  label: Entries
pages:
- _data/homepageShowcase.yml
